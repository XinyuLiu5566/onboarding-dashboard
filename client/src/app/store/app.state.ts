export interface AppState {
  employee: EmployeeState;
  hr: HrState;
  // Add more state properties for other modules or features
}

export interface EmployeeState {
  // Define the state structure for the employee module
  // Include properties relevant to the employee module
}

// Add more state interfaces for other modules or features
export interface HrState {}
